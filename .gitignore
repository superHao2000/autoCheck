# ── IDE 与本地工作区 ───────────────────────────────────────────────────────
# 各编辑器生成的项目设置仅属于本机，不参与协作。
/.idea/
/shelf/
/workspace.xml
# 生产分支不跟踪智能体技能、规范及会话状态。
/.agents/
/AGENTS.md
# Skills 锁文件只保留在本机，不参与版本管理。
/skills-lock.json

# ── Python 运行环境与缓存 ──────────────────────────────────────────────────
# 虚拟环境和字节码可由 requirements.txt 重新生成。
/.venv/
/venv/
__pycache__/
*.py[cod]

# ── 日志与临时调试文件 ─────────────────────────────────────────────────────
# 运行日志和个人调试脚本不应进入版本库。
/logs/
*.log
test.py

# ── 本地敏感配置与备份 ─────────────────────────────────────────────────────
# 真实账号、Cookie、推送凭据和历史备份只保留在本地。
config.json
config.json.bak
config/*.json

# 仅提交无凭据示例，方便其他使用者复制后填写自己的本地配置。
!config/services/*.example.json
