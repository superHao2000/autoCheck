# AutoCheck 项目规范

本文件是本项目智能体的唯一项目级规范来源。每次修改代码、配置、测试、文档或 Git 状态前，必须先完整阅读本文件。

## 文件保护与语言

- `AGENTS.md` 默认只读；只有用户在当前对话中直接、明确要求修改时才可变更。
- 面向用户的回复和新增 Markdown 文档默认使用简体中文；代码、路径、命令和配置标识符保持原样。
- 用户的当前直接请求决定任务范围；保留无关的既有改动，不擅自扩展任务。

## 代码结构

- 每个签到网站在 `checkin/` 下使用独立模块，站点间的请求、登录、响应判断和异常互不影响。
- 新增服务仅增加服务模块及对应 JSON 配置模板/本地配置，不修改 `main.py`。
- 服务必须声明 `SERVICE_NAME`、`CONFIG_FILENAME`、`ENV_KEY` 和 `ACCOUNT_FIELDS`，并复用 `utils.service_runner.run_accounts()`。
- 公共逻辑位于 `utils/`，站点专属逻辑保留在对应服务模块；避免为抽象而耦合不同网站接口。
- 单个账号或服务失败不得阻断其他已配置服务。

## 变更范围与安全

- 代码修改仅限 README 已列出的现有目录或文件；新增目录或修改 README 未列出的范围前，必须获得用户明确确认。
- 配置仅使用 JSON，不新增或恢复 YAML。
- 本地运行读取 `config/services/*.json`；GitHub Actions 和青龙优先读取环境变量；未配置服务跳过。
- 配置仅接受 README 表格中列出的标准字段，不兼容旧字段名或别名。
- 只提交 `*.example.json` 模板；不得提交真实账号、密码、Cookie、推送凭据、备份配置或本地 JSON 配置。
- 不在日志、文档、测试输出或提交信息中暴露敏感凭据。

## 代码风格与测试

- 新增或修改的代码必须具有清晰、准确、不过度冗长的中文注释或 docstring。
- 使用清晰的类型标注、函数命名和统一结果结构：`total`、`success`、`failed`、`details`。
- 优先捕获明确的异常类型；只有服务或账号隔离边界可捕获广泛异常，且必须说明原因。
- 服务名称统一使用 `YuChen`。
- 四个服务应具有独立测试入口，并提供全部服务的汇总运行入口。
- 真实服务测试只读取本地 `config/services/*.json`，不得读取环境变量；普通离线测试不得意外访问网站。
- 修改后运行与变更匹配的测试、编译或格式检查，并报告可核验结果。
- 新增服务或配置方式时，同步更新 README 和 JSON 模板。

## Git 与协作

- 提交前先生成提交说明，取得用户明确批准后再创建或改写提交。
- 推送、创建 Pull Request、发布、部署或其他外部状态变更前，先确认目标和用户授权。
- 默认单智能体工作；仅在用户明确要求创建子智能体、启动团队或并行处理时，才创建边界清晰且可独立验证的子任务。
