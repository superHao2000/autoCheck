# AutoCheck 项目规范

本文件是 AutoCheck 的唯一项目级智能体规范。修改代码、配置、测试、文档或
Git 状态前必须完整阅读，并以用户在当前对话中的直接要求为最高优先级。

## 工作原则

- 严格围绕当前任务工作，保留与任务无关的既有改动，不擅自扩大范围。
- 只读检查、分析或审查不代表获得修改、提交、推送或发布授权。
- 面向用户的回复及新增 Markdown 文档默认使用简体中文；代码、命令、路径和
  配置标识符保持原样。
- `AGENTS.md` 默认只读，只有用户在当前对话中明确要求时才可修改。
- 默认只修改 README 已列出的现有路径；新增目录或修改 README 未有的章节前，
  必须取得用户明确确认。

## 当前项目结构

- `main.py` 是生产及汇总运行入口，会自动发现 `checkin/` 下符合约定的服务，
  新增服务不得通过修改服务列表来接入。
- `checkin/` 存放各网站独立签到模块。当前服务为 YuChen、GlaDos、AirPort
  和 JavBus。
- `utils/` 存放配置加载、账号执行、日志及通知等公共逻辑。
- `config/services/*.example.json` 是可提交的配置模板；本地真实配置位于
  `config/*.json`，并由 `.gitignore` 排除。
- `tests/` 同时包含离线测试和四个服务的独立真实测试入口。
- `.github/workflows/` 存放 GitHub Actions 工作流。

## 服务模块约定

- 每个网站使用一个独立模块，站点请求、认证、响应解析和错误判断不得耦合到
  其他网站。
- 服务模块必须声明 `SERVICE_NAME`、`CONFIG_FILENAME`、`ENV_KEY` 和
  `ACCOUNT_FIELDS`，并提供 `checkin()` 与 `run()`。
- `run()` 应复用 `utils.service_runner.run_accounts()` 完成字段校验、账号
  统计和失败隔离。
- 新增服务通常只需增加服务模块、JSON 模板和独立测试，不修改 `main.py`。
- 公共、稳定且被多个服务复用的逻辑放入 `utils/`；请求路径、响应字段和积分
  解析等站点专属逻辑保留在服务模块中。
- 单账号失败不得阻断同服务的其他账号，单服务失败不得阻断其他服务。
- 服务名称统一写作 `YuChen`，不得使用其他大小写形式。

## 配置规则

- 运行期配置只使用 JSON，不新增或恢复 YAML，也不兼容未在 README 中声明的
  旧字段或别名。
- 本地配置文件为 `config/<服务>.json`；模板为
  `config/services/<服务>.example.json`。
- 生产运行按以下优先级读取并合并账号：
  1. 单服务环境变量，例如 `YUCHEN_ACCOUNTS`；
  2. 聚合环境变量 `AUTOCHECK_ACCOUNTS`；
  3. 本地 `config/<服务>.json`。
- 重复账号只保留优先级更高的一项，日志可以提示发现重复账号，但不得输出用于
  去重的敏感值。
- 未提供配置、账号数组为空或配置无效的服务应记录原因并跳过，不影响其他服务。
- `main.py --local-only` 和独立真实测试只读取本地 JSON，不读取账号环境变量。

## 安全要求

- 只允许提交 `*.example.json` 模板；不得提交真实账号、密码、Cookie、Token、
  推送凭据、备份配置或本地聚合配置。
- 不得在日志、通知、README、测试输出、异常信息或提交信息中暴露敏感凭据。
- 输出接口响应、请求参数或异常前必须进行脱敏；不得直接记录完整请求头、
  Cookie 或认证响应。
- 模板中的敏感字段必须为空或使用明显占位符，真实且稳定的公开站点 URL 可以
  写入模板。
- 不得将密码或 Token 拼接到 URL，避免被请求异常和代理日志记录。
- 真实签到会访问外部网站并可能改变账号状态，只在用户明确要求时执行。

## 代码风格

- 新增或修改的代码使用清晰的类型标注、准确命名以及简洁的中文注释或
  docstring；注释解释目的和关键约束，不逐行复述代码。
- 服务汇总结果统一使用 `total`、`success`、`failed` 和 `details`。
- 优先捕获明确的异常类型。只有账号或服务隔离边界可以捕获广泛异常，并应说明
  这是为了避免单点失败扩散。
- 优先使用现有公共函数消除重复，但不得为了抽象而让不同网站共享易变化的接口
  细节。
- 保持函数职责单一和控制流清晰，避免死代码、未使用导入、旧配置兼容分支及无
  实际需求的扩展点。

## 测试与验证

- 离线测试不得读取真实配置、环境变量中的账号凭据或意外访问外部网站。
- 全部离线测试命令：

  ```powershell
  .\.venv\Scripts\python.exe -m unittest discover -s tests -t . -v
  ```

- 四个服务的独立真实测试入口：

  ```powershell
  .\.venv\Scripts\python.exe -m tests.test_yuchen
  .\.venv\Scripts\python.exe -m tests.test_glados
  .\.venv\Scripts\python.exe -m tests.test_airport
  .\.venv\Scripts\python.exe -m tests.test_javbus
  ```

- 本地全部服务汇总入口：

  ```powershell
  .\.venv\Scripts\python.exe main.py --local-only
  ```

- 修改后必须运行与变更匹配的测试、编译或格式检查，并向用户报告实际命令、
  结果及未执行项目的原因。
- 真实测试日志可以展示签到结果、积分和脱敏后的失败原因，不得展示账号凭据或
  未清理的完整响应。

## 文档同步

- 行为、配置字段、命令或目录结构发生变化时，同步更新 README 中已有的相关
  章节；新增 README 章节必须先取得用户确认。
- 新增服务时同步增加 JSON 模板、独立离线测试及 README 的服务和配置说明。
- 文档中的命令必须可直接复制执行，示例配置必须是合法 JSON 且不含真实凭据。
- 保持 Markdown 标题层级连续、代码块标注语言、内部链接使用相对路径，并避免
  重复说明和大段空泛文字。

## Git 与协作

- 操作前检查当前分支和工作区，保留用户尚未提交的无关修改。
- 创建、切换、合并或删除分支必须符合用户当前请求，不自行改变分支策略。
- 创建或改写提交前，先展示拟提交文件、变更摘要和完整提交信息；只有取得用户
  明确批准后才能执行。
- 推送、强制推送、创建 Pull Request、发布或部署等外部状态变更必须单独获得
  用户授权。
- 提交前再次确认没有真实配置、日志、缓存、凭据或无关文件进入暂存区。
- 项目级 Skills 安装在 `.agents/skills/` 并纳入版本管理；`skills-lock.json`
  只保留在本机并由 `.gitignore` 排除。
- 默认单智能体工作；仅在用户明确要求启动团队、创建子智能体或并行处理时，
  才能拆分边界清晰且可独立验证的子任务。
